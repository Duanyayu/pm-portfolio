# Render 部署配置
# https://render.com → New Web Service → Connect GitHub repo
services:
  - type: web
    name: pm-dashboard
    env: node
    buildCommand: ""
    startCommand: node scripts/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "3000"
      - key: ADMIN_PASSWORD
        value: mars5admin2026
